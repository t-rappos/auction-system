
/*
let actionID = 0; //TODO: do we need this?
export const createAction = (object,customKey2) => {
  return {
    type: 'ACTION_NAME',
    customKey : {author : object.prop},
    customKey2,
    id: actionID++,
  };
};
*/
